# Zsh

Zsh as default shell.

For MacOS this is the default, but if using something else (Arch?)
then needs to be set via `chsh` after download.

Uses antigen as the package manager (`brew install antigen`)

```
ln -s dotfiles/zsh/zshrc ~/.zshrc
ln -s dotfiles/zsh/p10k.zsh ~/.zshrc
```
